# Optional: only needed to run the Google ADK agent through an LLM runtime.
GOOGLE_API_KEY=
ADK_MODEL=gemini-2.5-flash

# Browser-to-local-API route used by the demo dashboard.
NEXT_PUBLIC_API_URL=http://localhost:8000
